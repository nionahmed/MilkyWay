import { ChangefontStyleDirective } from './changefont-style.directive';

describe('ChangefontStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangefontStyleDirective();
    expect(directive).toBeTruthy();
  });
});
