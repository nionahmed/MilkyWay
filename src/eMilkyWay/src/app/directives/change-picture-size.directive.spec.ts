import { ElementRef } from '@angular/core';
import { ChangePictureSizeDirective } from './change-picture-size.directive';

describe('ChangePictureSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ChangePictureSizeDirective();
    expect(directive).toBeTruthy();
  });
});
