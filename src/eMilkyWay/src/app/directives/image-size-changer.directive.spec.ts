import { ImageSizeChangerDirective } from './image-size-changer.directive';

describe('ImageSizeChangerDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageSizeChangerDirective();
    expect(directive).toBeTruthy();
  });
});
