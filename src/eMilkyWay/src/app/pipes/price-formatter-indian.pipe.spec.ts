import { PriceFormatterIndianPipe } from './price-formatter-indian.pipe';

describe('PriceFormatterIndianPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceFormatterIndianPipe();
    expect(pipe).toBeTruthy();
  });
});
